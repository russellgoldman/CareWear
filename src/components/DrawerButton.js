import React, { Component } from 'react';
import { View, Text } from 'react-native';

class DrawerButton extends Component {
  render() {
    return (
      <View>
        <Text>{`I am a Drawer Button`}</Text>
      </View>
    );
  };
}

export default DrawerButton;
